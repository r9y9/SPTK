def configure(conf):
    pass

def build(bld):
    bld.install_files('${HPREFIX}', '../include/SPTK.h')

    swipe_src = [        
        'pitch/swipe/swipe.c',
        'pitch/swipe/vector.c',
    ]

    bld.shlib(
        source = bld.path.ant_glob('**/_*.c') + swipe_src,
        target = 'SPTK',
        vnum = bld.env['VERSION'],
        use = 'SPTKlib',
        includes = ['../include'])
